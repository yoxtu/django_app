def sum (hourly_wage,hour,minutes,a = 1):
    return int(hourly_wage) * ((int(minutes) / 60) + int(hour)) * a
def  sum2 (hourly_wage,hour,minutes,a = 1):
    return int(hourly_wage) * ((int(minutes) / 60) + int(hour)) * a
